input {
  beats {
    port => 5044
  }
}

output {
  opensearch {
    hosts => ["<%= @destination_url -%>"]
    index => "metricbeat-%{+YYYY.MM.dd}"
    user => "logstash"
    password => "logstash"
    ssl => true
    ssl_certificate_verification => false
  }
}