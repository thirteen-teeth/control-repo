input {
  beats {
    port => 5044
  }
}

output {
  opensearch {
    hosts => ["<%= @destination_url -%>"]
    index => "beats-%{+YYYY.MM.dd}"
    user => "admin"
    password => "admin"
    ssl => true
    ssl_certificate_verification => false
  }
}